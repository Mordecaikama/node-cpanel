on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v2.3.2

      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.0.0
        with:
          server: ftp.kamaservices.org
          username: username@kamaservices.org
          password: ;O6Ec$&^KcJM
          server-dir: /home/kamaiolo/kamaservices.org
